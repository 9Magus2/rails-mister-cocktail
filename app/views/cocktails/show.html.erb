<h2><strong>Cocktail:<strong></h2>
<ul>
  <% @cocktail.doses.each do |dose| %>
    <%= dose.description %>
    <%= dose.ingredient.name %>
  <% end %>
</ul>


<%= link_to 'Add a new ingredient', new_cocktail_dose_path(@cocktail), class: "btn btn-primary" %>